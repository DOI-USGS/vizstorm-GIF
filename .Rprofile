# Load fonts used at the start of every session, otherwise it won't be able to use them
# Will need to change if the fonts use change

message("Loading font 'Abel' from .Rprofile")
sysfonts::font_add_google('Abel', 'abel')
